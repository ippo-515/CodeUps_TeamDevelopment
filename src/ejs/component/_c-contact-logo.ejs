<% 
  const logoLink = "#";
  const logoSrc = "./assets/images/common/contact-logo.svg";
  const logoAlt = "CodeUps";
%>

<div class="c-contact-logo">
  <a href="<%= linkUrl %>">
    <img 
      src="<%= imagePath %>" 
      alt="<%= altText %>" 
      width="<%= imageWidth %>" 
      height="<%= imageHeight %>" 
      loading="lazy" 
      decoding="async">
  </a>
</div>
