<%
const { link, text, modifier } = locals || {};
%>

<a href="index.html" class="c-button <%= modifier ? `c-button--${modifier}` : '' %>">
  <span class="c-button__text <%= modifier ? `c-button__text--${modifier}` : '' %>"><%= text %></span>
</a>
